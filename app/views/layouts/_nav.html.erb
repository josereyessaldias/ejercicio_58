<div class="bg-light nav-top barrita">
  
    <div class="nav-inicio">
      <%= link_to 'Inicio', root_path %>
    </div>
  

    <% if user_signed_in? %>

        <div class="nav-topleft">

          <strong><%= link_to current_user.name, user_path(current_user), :class => 'nav-elemento' %></strong>

          <%= link_to 'Promocionar Actividad', new_promote_activity_path, :class => 'nav-elemento' %>

          <%= link_to 'Historial Promociones', promote_activity_path(current_user.id), :class => 'nav-elemento' %>

          <%= link_to 'Salir', destroy_user_session_path, method: :delete, :class => 'nav-elemento' %>

       </div>
    <% else %>

      <div class="nav-topleft">

        <%= link_to 'Ingresar', new_user_session_path, :class => 'nav-elemento' %>

        <%= link_to 'Registrarse', new_user_registration_path, :class => 'nav-elemento' %>
      
      </div>
    <% end %>

  
</div>